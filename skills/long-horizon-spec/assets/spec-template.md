# SPEC: <任务名称>

> 这份 SPEC 是执行的唯一参照。它必须自洽：点名文件与接口、写明不做什么、以一个端到端验证步骤收尾。
> 用户确认本文件之前，不开始改代码。

## 1. 目标（要做什么 / 为什么）

<用一两句话说清这次要达成的结果，以及它解决的问题。>

## 2. 范围

### 要做（In scope）
- <明确要交付的点 1>
- <明确要交付的点 2>

### 不做（Out of scope）
- <这次明确不碰的点 1，防止 scope 蔓延>
- <这次明确不碰的点 2>

## 3. 涉及的文件与接口

| 文件 / 模块 | 改动 | 说明 |
| --- | --- | --- |
| `path/to/file` | 新增 / 修改 | <做什么> |

参考的现有模式：`<指向一个可模仿的现有实现>`
约束：<不引入哪些依赖 / 必须兼容什么 / 性能或风格要求>

## 4. 方案要点与取舍

<选定的实现路径，以及为什么不选其他方案。记下访谈中敲定的关键决策。>

## 5. 边界情况

- <必须处理的边界 1：超时 / 并发 / 空数据 / 权限 / 失败回滚……>
- <必须处理的边界 2>

## 6. 验收标准（可运行的 check）

> 这是最关键的一节。每条都要能产出"通过/失败"信号，让 Claude 自己跑、自己读结果、自己迭代到通过。

- **check 类型**：测试 / 构建退出码 / linter / 输出 diff 脚本 / 截图对比
- **怎么跑**：`<具体命令，例如 npm test path/to/test>`
- **通过的判据**：
  - <用例 1：输入 → 期望输出>
  - <用例 2：输入 → 期望输出>
- **要求拿证据**：交付时附上跑了什么命令、返回了什么 / 测试输出 / 截图。

### 自主引擎（执行前和用户拍板）

- **本次挂哪个引擎**：<不挂（单 turn，人验收）/ `/goal` / Stop hook / Dynamic Workflow>
- **终止条件**：<原样引用上面的 check，不另行发明。挂 `/goal` 时写成可核对的形式："跑了哪条命令、看到什么输出"才算达成>
- **无人值守跑完后的复查**：<引擎越自主越不能省——交付前由独立 context 的 subagent 对着本 SPEC 验一遍>

### 仅"实验型任务"填（优化 / 打基线 / 调到指标达标）

> 如果这个任务是"反复试→量→比直到指标达标"，下面三项让这份 SPEC 能被 `engineering-loop` 直接当 Task Contract 的种子读入（字段已对齐）。普通搭功能/迁移任务跳过本节。

- **当前基线 + 出处（immutable baseline + provenance）**：<拿什么当对照？它是哪来的、怎么复现？开干前锁死，之后不许改。>
- **目标指标怎么测（target metric + evaluation command）**：<指标是什么，用哪条命令在与基线相同的条件下测出来。>
- **多好算赢（promotion criteria）**：<达到什么阈值/提升才算一个候选可以晋级；何时停。>

## 7. 端到端验证步骤

<一段从头到尾证明功能跑通的操作：启动到哪、点哪里 / 跑哪个命令、看到什么现象就算成功。>
